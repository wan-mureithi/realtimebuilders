import React from "react";

const HeroSection = () => {
  return (
    <div className="hero_section w-full bg-center">
      <div>
      <h1 className="hero_text">Quality craftmanship, one beam at a time</h1>
      </div>
    </div>
  );
};

export default HeroSection;
